## FEATURES
+ если удалить стартовую ноду вопроса, надо показывать модалку, что укажите стартовую ноду
+ показывать checkbox onlychoice только, если есть связи с choice
+ реализовать автолэйоут - вертикальный и горизонтальный
будем ли хранить позиции элементов в базе данных, чтобы можно было сохранять расположение? - скорее всего - нет
+ добавить кнопку - очистить холст
+ очистка холста - через модалку
+ показывать модалку с сообщениями, если пользователь делает что-то не так

как определять порядок ответов? - по координатам, слева направо
запросить контакт - перенести в меню?

появление нод - чтобы не налипали друг на друга (частичо сделал)
появление (добавление) через плюсик
+ удаление связей через кнопку с крестиком на этой связи

сделать тултип на корону
сделать тултип на текст с чекбоксом?
сделать тултип на текст с чекбоксом?


**Низкий приоритет
удаление вопроса через модалку (низкий приоритет). Надо ли вообще это, учитывая, что это все легко редактируется
удаление ответа - через модалку (низкий приоритет). Надо ли вообще это, учитывая, что это все легко редактируется
создать дубликат вопроса (из контекстного меню)
создать дубликат ответа (из контекстного меню)
реализовать возможность переподключать Edges - reconnect-edge - https://reactflow.dev/examples/edges/reconnect-edge. А надо ли?


## STYLES
+ сделал textarea resize: none
- сделать border - яркого цвета, чтобы было видно выделеную ноду


## REFACTOR
рефактор - валидация через isValidConnection https://reactflow.dev/examples/interaction/validation
+ useNodesState, useEdgesState
+ генерация айдишников uiid
+ перевести все на zustand
инлайн стили (styles) - перевести в нормальный вид
возможно, использовать css-переменные


## BUGS
цвет ручки drag/drop не понятно как определяется
aria-hidden в консоли
- если жмешь на замочек, то


## Валидация при соединении
+нельзя соединять вопрос с ответом, если уже есть соединение с вопросом
+нельзя соединять вопрос с вопросом, если уже есть соединение с ответом
+нельзя соединять ответ с ответом
сделать этот код переиспользуемым, чтобы можно было использовать в другом месте
как получать данные из ref внутри кастомных нод?
- на каком уровне импортировать стили? Если импортировать стили внутри DiagamBuilderб не будет ли дубликатов - скорее всего на уровне App, там же, где и ReactFlowProvider

## Валидация перед отправкой

prevent-cycles - https://reactflow.dev/examples/interaction/prevent-cycles
валидация перед отправкой (что есть стартовая нода)
- нет стартовой ноды
- стартовая нода - не является первой нодой, то есть часть диаграммы не будет срабатывать при запуске бота

## Производительность
- ререндеры при изменении внутреннего состояния - частично поборол, но вилимо, надо выпиливать хук useReactFlow, и все передавать через пропсы


## Настройки
- какой minZoom задавать?
















## Вопросы:
- как лучше валидировать? - не разрешать соединять или разрешать ипоказывать модалку (элемент обучения)
- какие сделать размеры кружочка (чтобы удобно было создавать связи) для создания связей
- перетаскивание - ОК или сделать отдельную зону, за которую можно еретаскивать
- ux куда перенести поле response??? Это поможет уменьшить размер, зафиксировать высоту - важно для autolayout
- какую иконку показывать - корону или дерево - для стартовой ноды
- какой вариант по умолчанию - onlyChoices (разрешить выбор только из предложенных вариантов, разрешить ответ в свободной форме)
- есть ли лагания, когда пользуетесь?
- тексты модалок - предложения?
- в каком виде располагать график - горизонтально или вертикально?

+ если запрошен контакт - то это последний вопрос? могут ли быть дальше еще вопросы, после того как запрошен контакт? - могут быть еще ответсвления
